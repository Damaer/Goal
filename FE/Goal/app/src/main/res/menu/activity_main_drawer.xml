<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:id="@+id/nav_group_main" android:checkableBehavior="single">
        <item
                android:id="@+id/nav_home"
                android:title="@string/nav_menu_home"
                android:icon="@drawable/ic_menu_home"/>
        <item
                android:id="@+id/nav_daily_sentence"
                android:icon="@drawable/ic_menu_daily_sentence"
                android:title="@string/nav_menu_daily_sentence"/>
        <item
                android:id="@+id/nav_statistics"
                android:icon="@drawable/ic_menu_statistics"
                android:title="@string/nav_menu_statistics"/>
        <item
                android:id="@+id/nav_contact"
                android:icon="@drawable/ic_menu_contact"
                android:title="@string/nav_menu_contact"/>
    </group>

    <group android:id="@+id/nav_group_setting" android:checkableBehavior="single">
        <item
                android:id="@+id/nav_settings"
                android:title="@string/nav_menu_setting"
                android:icon="@drawable/ic_menu_setting"/>
    </group>

</menu>
